import 'package:flutter/material.dart';

class ResponseBoard extends StatelessWidget {
  const ResponseBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
