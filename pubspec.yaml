name: flutter_serial
description: A flutter project for communicating with serial devices


publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_libserialport: ^0.2.2
  provider: ^6.0.0


  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true
